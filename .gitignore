__pycache__/
*.py[cod]
.coverage
htmlcov/
.pytest_cache/

# Cards and screenshots are the commander's own records, not the plugin.
cards/
data/
lab/

# Vendored at some point for a database path that no longer exists. Kept out
# of the repo until something imports it again.
lib/

# Release zips, built from a tag and uploaded, not kept.
RhinoSpotter-*.zip

# Local plans and todos, not part of the plugin.
plan/

# Instructions for Claude Code, not part of the plugin.
CLAUDE.md
rs_e2etest/out/

# Standalone installer build output.
installer/build/
installer/dist/
